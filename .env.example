# Contact form → Telegram relay (app/api/contact/route.ts).
# Both values are read on the server only; they are never sent to the browser.
#
# TELEGRAM_BOT_TOKEN — from @BotFather
# TELEGRAM_CHAT_ID   — the chat that should receive form submissions
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
